#
# Configuration for the HTTP package archive.
#
server {
    listen 80 ssl default_server;
    listen [::]:80 ssl default_server;

    root /srv/;

    location archive {
        autoindex on;
    }
}
