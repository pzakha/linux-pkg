#
# Configuration for the HTTP package archive.
# Any valid package archive that is put under /srv/archive will be accessible
# by apt through HTTP.
#
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /srv/;

    location /archive {
        # Allow browsing files under /archive
        autoindex on;
    }
}
